import {
  endProgress,
  initProgress,
  selectUiProgress,
  updateProgress,
} from "./progress";

export const progress = {
  end: endProgress,
  init: initProgress,
  select: selectUiProgress,
  update: updateProgress,
};
