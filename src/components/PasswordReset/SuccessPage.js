import React from "react";

const SuccessPage = () => (
  <div className="page password-reset-next-step-page">
    <p>Your password has been changed. You can now login with your new password.</p>
  </div>
);

export default SuccessPage;
